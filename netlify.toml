[build]
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist"  # Change this to your actual output folder

[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"
  NODE_VERSION = "16"